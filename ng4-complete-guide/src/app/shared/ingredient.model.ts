export class Ingredient {
    // Shortcut: ha ilyen módon néz a konstruktor, akkor nem kellenek
    // külön az attribútumok + beállítások
    constructor(public name: string, public amount: number) {}
}